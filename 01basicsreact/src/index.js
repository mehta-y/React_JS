import React from 'react';
import ReactDOM from 'react-dom/client';   // reactdom is an implementation of react on web page
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 
    <App />
  
);
